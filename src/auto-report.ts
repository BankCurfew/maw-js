// Auto-report to Bob when oracle sessions end — stub module

import type { FeedTailer } from "./feed-tail";

export function installAutoReport(_feedTailer: FeedTailer) {
  // LAW #7: Auto-report stub — will be implemented when needed
  // Watches feed for session-end events and notifies Bob if oracle didn't cc
}
